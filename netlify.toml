[build]
  command = "npx vite build"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "20"

# SPA redirect — all routes must serve index.html so wouter can handle routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
